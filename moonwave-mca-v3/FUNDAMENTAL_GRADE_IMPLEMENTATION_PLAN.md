# Moonwave Fundamental Grade 자동화 구현 전략

> **목표**: 새 종목 추가 시 Fundamental Grade가 자동 계산되도록 시스템 구축
> **제약조건**: API Key 사용 불가, 외부 크롤링 최소화
> **원칙**: Zero-API, 스마트 입력, 자동 계산

---

## Executive Summary

### 핵심 과제

```
┌─────────────────────────────────────────────────────────────────┐
│  Fundamental Grade 자동화의 핵심 도전                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [문제]                                                         │
│  13개 평가 항목에 필요한 데이터를 어떻게 수집할 것인가?          │
│                                                                 │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐       │
│  │ PER, PBR   │     │ 배당 정보   │     │ 자사주 현황 │       │
│  │ 밸류에이션 │     │ 주주환원    │     │ 공시 데이터 │       │
│  └──────┬──────┘     └──────┬──────┘     └──────┬──────┘       │
│         │                   │                   │               │
│         └───────────────────┼───────────────────┘               │
│                             ▼                                   │
│                    ┌─────────────────┐                         │
│                    │  API Key 없이   │                         │
│                    │  데이터 수집?   │                         │
│                    └─────────────────┘                         │
│                                                                 │
│  [해결책]                                                       │
│  1. 스마트 수동 입력 시스템                                     │
│  2. 클립보드 자동 파싱                                          │
│  3. 공개 데이터 활용 (KRX Open API - 무료)                      │
│  4. 커뮤니티 데이터 공유                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 솔루션 개요

| 접근법 | 데이터 항목 | 자동화 수준 | 구현 복잡도 |
|--------|-----------|------------|------------|
| **KRX 정보데이터시스템** | PER, PBR, 배당수익률 | 완전 자동 | 중간 |
| **클립보드 파싱** | 전체 13개 항목 | 반자동 | 낮음 |
| **스마트 입력 폼** | 전체 13개 항목 | 수동 (가이드) | 낮음 |
| **템플릿 시스템** | 인기 종목 사전 입력 | 완전 자동 | 낮음 |

---

## Part 1: 데이터 소스 분석

### 1.1 API Key 없이 접근 가능한 공개 데이터

#### KRX 정보데이터시스템 (data.krx.co.kr)

**특징**: 한국거래소 공식 데이터, **무료**, API Key 불필요

```
┌─────────────────────────────────────────────────────────────────┐
│  KRX 정보데이터시스템 - 무료 공개 데이터                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [접근 방식]                                                     │
│  URL: http://data.krx.co.kr/contents/MDC/MDI/mdiLoader          │
│  방식: POST 요청 (OTP 발급 후 데이터 조회)                       │
│                                                                 │
│  [사용 가능 데이터]                                              │
│  ✅ PER (주가수익비율)                                          │
│  ✅ PBR (주가순자산비율)                                        │
│  ✅ 배당수익률                                                  │
│  ✅ 시가총액                                                    │
│  ✅ 발행주식수                                                  │
│  ✅ 업종 정보                                                   │
│                                                                 │
│  [제한 사항]                                                     │
│  ⚠️ CORS 제한 (프록시 또는 서버 필요)                           │
│  ⚠️ 자사주, 배당이력 등 상세 데이터 없음                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**KRX 데이터 활용 전략**:

| 데이터 | KRX 엔드포인트 | 활용 |
|--------|---------------|------|
| PER/PBR | 12021 (개별종목시세) | 밸류에이션 자동 계산 |
| 배당수익률 | 12021 | 배당 점수 자동 계산 |
| 업종분류 | 12003 | 업종 평균 비교 |

**CORS 우회 방안**:

```
옵션 1: GitHub Actions Proxy
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│   MCA App   │ ──→ │ GitHub Pages │ ──→ │  KRX Data   │
│  (Client)   │     │  Functions   │     │   Server    │
└─────────────┘     └──────────────┘     └─────────────┘

옵션 2: Cloudflare Workers (무료 티어)
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│   MCA App   │ ──→ │  Cloudflare  │ ──→ │  KRX Data   │
│  (Client)   │     │   Worker     │     │   Server    │
└─────────────┘     └──────────────┘     └─────────────┘

옵션 3: 사용자 로컬 프록시 (개발자용)
- 브라우저 확장 프로그램으로 CORS 우회
- 고급 사용자 전용 옵션
```

---

### 1.2 Fundamental Grade 항목별 데이터 소스 매핑

| No. | 항목 | 배점 | 자동화 가능 | 데이터 소스 | 대안 |
|-----|------|------|------------|-------------|------|
| 1 | PER | 20점 | ✅ 가능 | KRX 정보데이터 | 클립보드 파싱 |
| 2 | PBR | 5점 | ✅ 가능 | KRX 정보데이터 | 클립보드 파싱 |
| 3 | 이익 지속성 | 5점 | ⚠️ 부분 | 수동 입력 | 클립보드 파싱 |
| 4 | 중복 상장 | 5점 | ❌ 수동 | 수동 입력 | 템플릿 DB |
| 5 | 배당수익률 | 10점 | ✅ 가능 | KRX 정보데이터 | 클립보드 파싱 |
| 6 | 분기 배당 | 5점 | ❌ 수동 | 수동 입력 | 템플릿 DB |
| 7 | 배당 연속 인상 | 5점 | ❌ 수동 | 수동 입력 | 템플릿 DB |
| 8 | 자사주 매입/소각 | 7점 | ❌ 수동 | 수동 입력 | 클립보드 파싱 |
| 9 | 연간 소각 비율 | 8점 | ❌ 수동 | 수동 입력 | 클립보드 파싱 |
| 10 | 자사주 보유 비율 | 5점 | ❌ 수동 | 수동 입력 | 클립보드 파싱 |
| 11 | 성장 잠재력 | 10점 | ❌ 정성적 | 사용자 판단 | 가이드 제공 |
| 12 | 경영진 리스크 | 10점 | ❌ 정성적 | 사용자 판단 | 체크리스트 |
| 13 | 글로벌 브랜드 | 5점 | ❌ 정성적 | 사용자 판단 | 템플릿 DB |

**자동화 가능 점수**: 35점 / 100점 (PER 20 + PBR 5 + 배당수익률 10)

---

## Part 2: 스마트 입력 시스템 설계

### 2.1 종목 추가 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│  종목 추가 시 Fundamental Grade 입력 플로우                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 1: 기본 정보 입력                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  종목명: [삼성전자        ]                              │   │
│  │  종목코드: [005930   ] ← 자동완성                       │   │
│  │  고점가격: [85,000원     ]                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          ▼                                      │
│  Step 2: 자동 데이터 조회 (KRX)                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔄 KRX에서 데이터 조회 중...                           │   │
│  │                                                         │   │
│  │  ✅ PER: 12.5배 (자동)                                  │   │
│  │  ✅ PBR: 1.2배 (자동)                                   │   │
│  │  ✅ 배당수익률: 2.3% (자동)                             │   │
│  │  ⚠️ 나머지 항목은 수동 입력이 필요합니다                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          ▼                                      │
│  Step 3: 추가 데이터 입력 (가이드 제공)                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  📋 클립보드에서 붙여넣기 [Ctrl+V로 자동 파싱]          │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  또는 개별 항목 입력:                                   │   │
│  │                                                         │   │
│  │  [밸류에이션]                                           │   │
│  │  • 이익 지속성: [5년 연속 흑자 ▾] → 5점                │   │
│  │  • 중복 상장: [단독 상장 ▾] → 5점                      │   │
│  │                                                         │   │
│  │  [주주환원]                                             │   │
│  │  • 분기 배당: [미실시 ▾] → 0점                         │   │
│  │  • 배당 연속 인상: [해당 없음 ▾] → 0점                 │   │
│  │  • 자사주 매입/소각: [미실시 ▾] → 0점                  │   │
│  │  • 연간 소각 비율: [0.5% 미만 ▾] → 0점                 │   │
│  │  • 자사주 보유 비율: [5% 초과 ▾] → 0점                 │   │
│  │                                                         │   │
│  │  [성장/경영]                                            │   │
│  │  • 성장 잠재력: [높음 ▾] → 7점                         │   │
│  │  • 경영진: [우수한 경영자 ▾] → 10점                    │   │
│  │  • 글로벌 브랜드: [보유 ▾] → 5점                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          ▼                                      │
│  Step 4: 자동 점수 계산 및 Grade 판정                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  📊 Fundamental Grade 계산 결과                         │   │
│  │                                                         │   │
│  │  밸류에이션:  15/35점 ███████░░░░░░░░░                  │   │
│  │  주주환원:    12/40점 ██████░░░░░░░░░░░░░░             │   │
│  │  성장/경영:  22/25점 █████████████████████░░           │   │
│  │  ───────────────────────────────────────────────────   │   │
│  │  총점:       49/100점                                   │   │
│  │                                                         │   │
│  │  🔴 Grade D - 매수 금지                                 │   │
│  │  "50점 미만 종목은 어떤 조건에서도 매수하지 않습니다"   │   │
│  │                                                         │   │
│  │  [저장] [다시 입력] [취소]                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### 2.2 클립보드 자동 파싱 시스템

**개념**: 네이버 증권/컴퍼니가이드에서 데이터를 복사하면 자동으로 파싱

```typescript
// 클립보드 데이터 파싱 예시
interface ParsedFinancialData {
  per?: number;        // "PER 12.5배" → 12.5
  pbr?: number;        // "PBR 1.2배" → 1.2
  dividendYield?: number; // "배당수익률 2.3%" → 2.3
  treasuryStock?: number; // "자사주 5.2%" → 5.2
}

function parseClipboardData(text: string): ParsedFinancialData {
  const result: ParsedFinancialData = {};

  // PER 파싱
  const perMatch = text.match(/PER[:\s]*([0-9,.]+)/i);
  if (perMatch) result.per = parseFloat(perMatch[1].replace(',', ''));

  // PBR 파싱
  const pbrMatch = text.match(/PBR[:\s]*([0-9,.]+)/i);
  if (pbrMatch) result.pbr = parseFloat(pbrMatch[1].replace(',', ''));

  // 배당수익률 파싱
  const divMatch = text.match(/배당수익률[:\s]*([0-9,.]+)/);
  if (divMatch) result.dividendYield = parseFloat(divMatch[1]);

  return result;
}
```

**지원 형식**:

| 소스 | 복사 예시 | 파싱 결과 |
|------|----------|----------|
| 네이버 증권 | "PER 12.5배 PBR 1.2배" | `{per: 12.5, pbr: 1.2}` |
| 컴퍼니가이드 | "배당수익률 2.30%" | `{dividendYield: 2.3}` |
| 텍스트 테이블 | "PER\t12.5\nPBR\t1.2" | `{per: 12.5, pbr: 1.2}` |

---

### 2.3 인기 종목 템플릿 시스템

**개념**: 자주 분석되는 종목의 데이터를 사전 입력하여 제공

```typescript
interface StockTemplate {
  code: string;
  name: string;
  lastUpdated: string;
  fundamentalData: {
    // 밸류에이션 (시장 데이터 - 자동 갱신 필요)
    per?: number;
    pbr?: number;

    // 정적 데이터 (변경 빈도 낮음)
    earningsSustainability: boolean;  // 이익 지속성
    isDualListed: boolean;            // 중복 상장
    hasQuarterlyDividend: boolean;    // 분기 배당
    consecutiveDividendYears: number; // 배당 연속 인상 연수
    hasBuybackProgram: boolean;       // 자사주 프로그램
    annualCancellationRate: number;   // 연간 소각 비율
    treasuryStockRatio: number;       // 자사주 보유 비율
    growthPotential: 'very_high' | 'high' | 'normal' | 'low';
    managementQuality: 'excellent' | 'professional' | 'owner_risk';
    hasGlobalBrand: boolean;
  };
}

// 예시 템플릿
const STOCK_TEMPLATES: StockTemplate[] = [
  {
    code: '005930',
    name: '삼성전자',
    lastUpdated: '2026-01-20',
    fundamentalData: {
      earningsSustainability: true,
      isDualListed: true,  // 삼성물산과 중복
      hasQuarterlyDividend: true,
      consecutiveDividendYears: 5,
      hasBuybackProgram: true,
      annualCancellationRate: 1.2,
      treasuryStockRatio: 3.5,
      growthPotential: 'high',
      managementQuality: 'professional',
      hasGlobalBrand: true,
    }
  },
  // ... 더 많은 템플릿
];
```

**템플릿 관리 전략**:

```
┌─────────────────────────────────────────────────────────────────┐
│  템플릿 데이터 관리 전략                                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [초기 제공 템플릿] - 앱 내장                                    │
│  ├── KOSPI 시가총액 상위 30종목                                 │
│  ├── KOSDAQ 시가총액 상위 20종목                                │
│  └── 고배당주 20종목                                            │
│                                                                 │
│  [사용자 기여 템플릿] - 커뮤니티                                 │
│  ├── JSON 형식으로 내보내기/가져오기                            │
│  ├── QR 코드로 공유                                             │
│  └── 버전 관리 (lastUpdated)                                    │
│                                                                 │
│  [자동 갱신] - KRX 데이터                                        │
│  ├── PER, PBR, 배당수익률만 자동 갱신                           │
│  └── 정적 데이터는 분기별 수동 검토                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Part 3: 자동 점수 계산 엔진

### 3.1 스코어링 엔진 설계

```typescript
// types/fundamentalGrade.ts
export interface FundamentalInput {
  // 카테고리 I: 밸류에이션
  per: number;              // PER 배수
  pbr: number;              // PBR 배수
  earningsSustainability: boolean;  // 이익 지속성
  isDualListed: boolean;    // 중복 상장

  // 카테고리 II: 주주환원
  dividendYield: number;    // 배당수익률 (%)
  hasQuarterlyDividend: boolean;  // 분기 배당
  consecutiveDividendYears: number;  // 배당 연속 인상 연수
  hasBuybackProgram: boolean;  // 자사주 매입/소각 실시
  annualCancellationRate: number;  // 연간 소각 비율 (%)
  treasuryStockRatio: number;  // 자사주 보유 비율 (%)

  // 카테고리 III: 성장/경영
  growthPotential: 'very_high' | 'high' | 'normal' | 'low';
  managementQuality: 'excellent' | 'professional' | 'owner_risk';
  hasGlobalBrand: boolean;
}

export interface FundamentalScore {
  // 개별 점수
  scores: {
    per: number;              // 0-20
    pbr: number;              // 0-5
    earningsSustainability: number;  // 0-5
    dualListing: number;      // 0-5
    dividendYield: number;    // 0-10
    quarterlyDividend: number;  // 0-5
    consecutiveDividend: number;  // 0-5
    buybackProgram: number;   // 0-7
    cancellationRate: number;  // 0-8
    treasuryStockRatio: number;  // 0-5
    growthPotential: number;  // 0-10
    management: number;       // 0-10
    globalBrand: number;      // 0-5
  };

  // 카테고리별 소계
  categoryScores: {
    valuation: number;        // 0-35
    shareholderReturn: number;  // 0-40
    growthManagement: number;  // 0-25
  };

  // 최종 결과
  totalScore: number;         // 0-100
  grade: 'A' | 'B' | 'C' | 'D';
  gradeLabel: string;         // "적극 매수", "매수 고려", etc.
  actionGuideline: string;    // 투자 행동 지침
}
```

### 3.2 점수 계산 로직

```typescript
// services/fundamentalGrade.ts
export function calculateFundamentalScore(input: FundamentalInput): FundamentalScore {
  const scores = {
    // PER (20점)
    per: input.per < 5 ? 20 :
         input.per < 8 ? 15 :
         input.per < 10 ? 10 : 5,

    // PBR (5점)
    pbr: input.pbr < 0.3 ? 5 :
         input.pbr < 0.6 ? 4 :
         input.pbr < 1.0 ? 3 : 0,

    // 이익 지속성 (5점)
    earningsSustainability: input.earningsSustainability ? 5 : 0,

    // 중복 상장 (5점)
    dualListing: input.isDualListed ? 0 : 5,

    // 배당수익률 (10점)
    dividendYield: input.dividendYield > 7 ? 10 :
                   input.dividendYield > 5 ? 7 :
                   input.dividendYield > 3 ? 5 : 2,

    // 분기 배당 (5점)
    quarterlyDividend: input.hasQuarterlyDividend ? 5 : 0,

    // 배당 연속 인상 (5점)
    consecutiveDividend: input.consecutiveDividendYears >= 10 ? 5 :
                         input.consecutiveDividendYears >= 5 ? 4 :
                         input.consecutiveDividendYears >= 3 ? 3 : 0,

    // 자사주 매입/소각 (7점)
    buybackProgram: input.hasBuybackProgram ? 7 : 0,

    // 연간 소각 비율 (8점)
    cancellationRate: input.annualCancellationRate > 2 ? 8 :
                      input.annualCancellationRate > 1.5 ? 5 :
                      input.annualCancellationRate > 0.5 ? 3 : 0,

    // 자사주 보유 비율 (5점) - 낮을수록 좋음
    treasuryStockRatio: input.treasuryStockRatio === 0 ? 5 :
                        input.treasuryStockRatio < 2 ? 4 :
                        input.treasuryStockRatio < 5 ? 2 : 0,

    // 성장 잠재력 (10점)
    growthPotential: input.growthPotential === 'very_high' ? 10 :
                     input.growthPotential === 'high' ? 7 :
                     input.growthPotential === 'normal' ? 5 : 3,

    // 경영진 (10점)
    management: input.managementQuality === 'excellent' ? 10 :
                input.managementQuality === 'professional' ? 5 : 0,

    // 글로벌 브랜드 (5점)
    globalBrand: input.hasGlobalBrand ? 5 : 0,
  };

  // 카테고리별 소계
  const categoryScores = {
    valuation: scores.per + scores.pbr + scores.earningsSustainability + scores.dualListing,
    shareholderReturn: scores.dividendYield + scores.quarterlyDividend +
                       scores.consecutiveDividend + scores.buybackProgram +
                       scores.cancellationRate + scores.treasuryStockRatio,
    growthManagement: scores.growthPotential + scores.management + scores.globalBrand,
  };

  // 총점
  const totalScore = categoryScores.valuation +
                     categoryScores.shareholderReturn +
                     categoryScores.growthManagement;

  // Grade 판정
  const grade = totalScore > 80 ? 'A' :
                totalScore >= 70 ? 'B' :
                totalScore >= 50 ? 'C' : 'D';

  const gradeLabels = {
    A: '적극 매수',
    B: '매수 고려',
    C: '신중 진입',
    D: '매수 금지',
  };

  const actionGuidelines = {
    A: 'Step 4(기술적 분석)로 무조건 진행. Zone 3/4 진입 시 MCA/DCA 가동.',
    B: 'Step 4 진행. 멀티플 1.2 이하 시 적극 고려.',
    C: 'Step 4 진행 가능. 멀티플 1.0 이하 + Zone 3 한정 + 소량 매수.',
    D: '어떤 조건에서도 매수 불가. 기존 보유 시 매도 또는 교체 권고.',
  };

  return {
    scores,
    categoryScores,
    totalScore,
    grade,
    gradeLabel: gradeLabels[grade],
    actionGuideline: actionGuidelines[grade],
  };
}
```

---

## Part 4: KRX 데이터 자동 조회 시스템

### 4.1 KRX 정보데이터시스템 연동 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│  KRX 데이터 조회 아키텍처                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [클라이언트 전용 방식] - CORS 프록시 활용                       │
│                                                                 │
│  MCA App                Cloudflare Worker            KRX        │
│  ┌─────────┐           ┌─────────────────┐      ┌──────────┐   │
│  │         │  fetch()  │                 │ POST │          │   │
│  │ React   │ ────────→ │  CORS Proxy     │ ───→ │ data.krx │   │
│  │ Client  │           │  (무료 티어)    │      │ .co.kr   │   │
│  │         │ ←──────── │                 │ ←─── │          │   │
│  └─────────┘   JSON    └─────────────────┘      └──────────┘   │
│                                                                 │
│  Cloudflare Worker 무료 티어:                                   │
│  - 일 100,000 요청 무료                                         │
│  - 전 세계 엣지 서버                                            │
│  - 별도 서버 운영 불필요                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 Cloudflare Worker 코드 (CORS 프록시)

```javascript
// cloudflare-worker/krx-proxy.js
export default {
  async fetch(request, env) {
    // CORS 헤더
    const corsHeaders = {
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
      'Access-Control-Allow-Headers': 'Content-Type',
    };

    // Preflight 요청 처리
    if (request.method === 'OPTIONS') {
      return new Response(null, { headers: corsHeaders });
    }

    try {
      const url = new URL(request.url);
      const stockCode = url.searchParams.get('code');

      if (!stockCode) {
        return new Response(JSON.stringify({ error: 'Stock code required' }), {
          status: 400,
          headers: { ...corsHeaders, 'Content-Type': 'application/json' },
        });
      }

      // KRX OTP 발급
      const otpResponse = await fetch(
        'http://data.krx.co.kr/comm/fileDn/GenerateOTP/generate.cmd',
        {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({
            locale: 'ko_KR',
            mktId: 'ALL',
            trdDd: getTodayDate(),
            share: '1',
            money: '1',
            csvxls_is498Y: 'false',
            name: 'fileDown',
            url: 'dbms/MDC/STAT/standard/MDCSTAT01501',
          }),
        }
      );

      const otp = await otpResponse.text();

      // 실제 데이터 조회
      const dataResponse = await fetch(
        'http://data.krx.co.kr/comm/fileDn/download_csv/download.cmd',
        {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: new URLSearchParams({ code: otp }),
        }
      );

      const csvData = await dataResponse.text();
      const stockData = parseCSVForStock(csvData, stockCode);

      return new Response(JSON.stringify(stockData), {
        headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      });
    } catch (error) {
      return new Response(JSON.stringify({ error: error.message }), {
        status: 500,
        headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      });
    }
  },
};

function getTodayDate() {
  const d = new Date();
  return `${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}`;
}

function parseCSVForStock(csv, code) {
  // CSV 파싱하여 특정 종목 데이터 추출
  const lines = csv.split('\n');
  for (const line of lines) {
    if (line.includes(code)) {
      const cols = line.split(',');
      return {
        code: cols[0]?.trim(),
        name: cols[1]?.trim(),
        per: parseFloat(cols[10]) || null,
        pbr: parseFloat(cols[11]) || null,
        dividendYield: parseFloat(cols[12]) || null,
      };
    }
  }
  return null;
}
```

### 4.3 클라이언트 서비스 코드

```typescript
// services/krxData.ts
const KRX_PROXY_URL = 'https://mca-krx-proxy.your-worker.workers.dev';

export interface KRXStockData {
  code: string;
  name: string;
  per: number | null;
  pbr: number | null;
  dividendYield: number | null;
}

export async function fetchKRXData(stockCode: string): Promise<KRXStockData | null> {
  try {
    const response = await fetch(`${KRX_PROXY_URL}?code=${stockCode}`);

    if (!response.ok) {
      throw new Error('KRX data fetch failed');
    }

    return await response.json();
  } catch (error) {
    console.error('KRX data fetch error:', error);
    return null;
  }
}

// 사용 예시
async function onStockCodeEntered(code: string) {
  const krxData = await fetchKRXData(code);

  if (krxData) {
    // 자동으로 폼 필드 채우기
    setFormValues({
      per: krxData.per ?? undefined,
      pbr: krxData.pbr ?? undefined,
      dividendYield: krxData.dividendYield ?? undefined,
    });
  }
}
```

---

## Part 5: UI/UX 설계

### 5.1 Fundamental Grade 입력 모달

```
┌─────────────────────────────────────────────────────────────────┐
│  📊 Fundamental Grade 입력                              [×]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  종목코드: [005930] 삼성전자  [🔄 데이터 조회]                  │
│                                                                 │
│  ┌─ 카테고리 I: 밸류에이션 (35점) ────────────────────────────┐ │
│  │                                                            │ │
│  │  PER          [12.5    ]배   → 5점/20점    ⓘ              │ │
│  │  PBR          [1.2     ]배   → 0점/5점     ⓘ              │ │
│  │  이익 지속성   [5년 연속 흑자 ▾]  → 5점/5점                │ │
│  │  중복 상장     [중복 상장 ▾]      → 0점/5점                │ │
│  │                                                            │ │
│  │  소계: 10/35점  ████████░░░░░░░░░░░░░░░░░░░░░░░░          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 카테고리 II: 주주환원 (40점) ─────────────────────────────┐ │
│  │                                                            │ │
│  │  배당수익률      [2.3     ]%   → 2점/10점                  │ │
│  │  분기 배당       [○ 실시  ● 미실시]  → 0점/5점             │ │
│  │  배당 연속 인상  [5년 이상 ▾]        → 4점/5점             │ │
│  │  자사주 매입/소각 [○ 실시  ● 미실시] → 0점/7점             │ │
│  │  연간 소각 비율  [0.5% 미만 ▾]       → 0점/8점             │ │
│  │  자사주 보유율   [2% 미만 ▾]         → 4점/5점             │ │
│  │                                                            │ │
│  │  소계: 10/40점  █████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌─ 카테고리 III: 성장/경영 (25점) ───────────────────────────┐ │
│  │                                                            │ │
│  │  성장 잠재력    [높음 ▾]              → 7점/10점           │ │
│  │  경영진 평가    [우수한 경영자 ▾]     → 10점/10점          │ │
│  │  글로벌 브랜드  [○ 보유  ○ 미보유]    → 5점/5점            │ │
│  │                                                            │ │
│  │  소계: 22/25점  █████████████████████████████░░░░░░░░░░░░ │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ═══════════════════════════════════════════════════════════   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │        총점: 42 / 100점                                │   │
│  │                                                         │   │
│  │        🔴 Grade D - 매수 금지                          │   │
│  │                                                         │   │
│  │  ⚠️ "50점 미만 종목은 어떤 조건에서도                   │   │
│  │      매수하지 않습니다"                                 │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  [저장하고 포트폴리오 추가]            [취소]                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 포트폴리오 카드에 Grade 표시

```
┌─────────────────────────────────────────────────────────────────┐
│  대시보드 - 포트폴리오 카드 디자인                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────┐                      │
│  │ ★ NAVER                     Grade B │  ← Grade 뱃지        │
│  │                            ┌──────┐ │                      │
│  │  진행률: ████████░░ 40%    │  72  │ │  ← 점수 원형        │
│  │                            │ /100 │ │                      │
│  │  투입 금액: 45.2M          └──────┘ │                      │
│  │  평균 단가: 228,450원                │                      │
│  │                                      │                      │
│  │  🟢 정상 운용                        │                      │
│  └──────────────────────────────────────┘                      │
│                                                                 │
│  Grade별 뱃지 색상:                                             │
│  ┌────────┬────────┬────────┬────────┐                        │
│  │Grade A │Grade B │Grade C │Grade D │                        │
│  │ 🟢녹색 │ 🔵파랑 │ 🟡노랑 │ 🔴빨강 │                        │
│  └────────┴────────┴────────┴────────┘                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Part 6: 데이터 모델 확장

### 6.1 IndexedDB 스키마 확장

```typescript
// types/index.ts 확장
export interface FundamentalData {
  // 밸류에이션
  per: number | null;
  pbr: number | null;
  earningsSustainability: boolean;
  isDualListed: boolean;

  // 주주환원
  dividendYield: number | null;
  hasQuarterlyDividend: boolean;
  consecutiveDividendYears: number;
  hasBuybackProgram: boolean;
  annualCancellationRate: number;
  treasuryStockRatio: number;

  // 성장/경영
  growthPotential: 'very_high' | 'high' | 'normal' | 'low';
  managementQuality: 'excellent' | 'professional' | 'owner_risk';
  hasGlobalBrand: boolean;

  // 메타데이터
  dataSource: 'manual' | 'krx' | 'template' | 'clipboard';
  lastUpdated: Date;
  notes?: string;
}

export interface Portfolio {
  id?: number;
  name: string;
  stockCode?: string;        // NEW: 종목코드
  isFavorite: boolean;
  createdAt: Date;
  updatedAt: Date;
  params: PortfolioParams;
  memo?: string;
  fundamentalData?: FundamentalData;  // NEW: Fundamental Grade 데이터
  fundamentalScore?: number;          // NEW: 계산된 총점
  fundamentalGrade?: 'A' | 'B' | 'C' | 'D';  // NEW: Grade
}

// services/database.ts 확장
class MCADatabase extends Dexie {
  portfolios!: Table<Portfolio>;
  trades!: Table<Trade>;
  settings!: Table<Settings>;
  history!: Table<HistoryEntry>;
  stockTemplates!: Table<StockTemplate>;  // NEW: 종목 템플릿

  constructor() {
    super('MoonwaveMCA_v3');

    this.version(2).stores({
      portfolios: '++id, name, stockCode, isFavorite, fundamentalGrade, updatedAt',
      trades: '++id, portfolioId, step, status',
      settings: 'id',
      history: '++id, timestamp, portfolioId',
      stockTemplates: '++id, code, name, lastUpdated',  // NEW
    });
  }
}
```

---

## Part 7: 구현 로드맵

### Phase 1: 기초 인프라 (3-4일)

| 작업 | 설명 | 의존성 |
|------|------|--------|
| 타입 정의 | FundamentalInput, FundamentalScore 타입 | 없음 |
| 스코어링 엔진 | calculateFundamentalScore 함수 | 타입 |
| DB 스키마 확장 | Portfolio에 fundamentalData 추가 | 타입 |
| 마이그레이션 | v3.0 → v3.1 DB 마이그레이션 | DB 스키마 |

### Phase 2: 입력 시스템 (4-5일)

| 작업 | 설명 | 의존성 |
|------|------|--------|
| FundamentalGradeModal | 입력 모달 컴포넌트 | 스코어링 엔진 |
| 클립보드 파싱 | 자동 데이터 파싱 로직 | 없음 |
| 스마트 입력 폼 | 드롭다운, 라디오버튼 등 | UI 컴포넌트 |
| 실시간 점수 계산 | 입력 시 즉시 점수 반영 | 스코어링 엔진 |

### Phase 3: KRX 연동 (2-3일)

| 작업 | 설명 | 의존성 |
|------|------|--------|
| Cloudflare Worker | CORS 프록시 배포 | 없음 |
| KRX 서비스 | fetchKRXData 함수 | CF Worker |
| 자동 조회 통합 | 종목코드 입력 시 자동 조회 | KRX 서비스 |
| 폴백 처리 | 조회 실패 시 수동 입력 | 입력 시스템 |

### Phase 4: UI 통합 (3-4일)

| 작업 | 설명 | 의존성 |
|------|------|--------|
| 포트폴리오 추가 플로우 | Grade 입력 단계 추가 | 모달 |
| 대시보드 Grade 표시 | 카드에 Grade 뱃지 | DB 스키마 |
| 상세 페이지 Grade 섹션 | Grade 정보 표시/수정 | 모달 |
| Grade별 필터링 | 대시보드 필터 추가 | DB 쿼리 |

### Phase 5: 템플릿 시스템 (2-3일)

| 작업 | 설명 | 의존성 |
|------|------|--------|
| 초기 템플릿 데이터 | 상위 50종목 데이터 | 없음 |
| 템플릿 선택 UI | 종목 선택 시 템플릿 적용 | 템플릿 데이터 |
| 템플릿 내보내기/가져오기 | JSON 공유 기능 | 연결성 전략 |

---

## Part 8: 검증 및 테스트

### 8.1 스코어링 엔진 테스트 케이스

```typescript
// tests/fundamentalGrade.test.ts
describe('calculateFundamentalScore', () => {
  it('Grade A 종목 (80점 초과)', () => {
    const input: FundamentalInput = {
      per: 4.5,  // 20점
      pbr: 0.25, // 5점
      earningsSustainability: true, // 5점
      isDualListed: false, // 5점
      dividendYield: 8, // 10점
      hasQuarterlyDividend: true, // 5점
      consecutiveDividendYears: 12, // 5점
      hasBuybackProgram: true, // 7점
      annualCancellationRate: 2.5, // 8점
      treasuryStockRatio: 0, // 5점
      growthPotential: 'high', // 7점
      managementQuality: 'excellent', // 10점
      hasGlobalBrand: true, // 5점
    };

    const result = calculateFundamentalScore(input);
    expect(result.totalScore).toBe(97);
    expect(result.grade).toBe('A');
  });

  it('Grade D 종목 (50점 미만)', () => {
    const input: FundamentalInput = {
      per: 15, // 5점
      pbr: 1.5, // 0점
      earningsSustainability: false, // 0점
      isDualListed: true, // 0점
      dividendYield: 1.5, // 2점
      hasQuarterlyDividend: false, // 0점
      consecutiveDividendYears: 0, // 0점
      hasBuybackProgram: false, // 0점
      annualCancellationRate: 0, // 0점
      treasuryStockRatio: 10, // 0점
      growthPotential: 'low', // 3점
      managementQuality: 'owner_risk', // 0점
      hasGlobalBrand: false, // 0점
    };

    const result = calculateFundamentalScore(input);
    expect(result.totalScore).toBe(10);
    expect(result.grade).toBe('D');
  });
});
```

---

## Part 9: 리스크 및 대안

### 9.1 KRX 데이터 조회 실패 시

```
┌─────────────────────────────────────────────────────────────────┐
│  데이터 조회 실패 시 폴백 전략                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1차 시도: KRX 정보데이터시스템                                  │
│      ↓ 실패                                                     │
│  2차 시도: 캐시된 템플릿 데이터 사용                             │
│      ↓ 없음                                                     │
│  3차 시도: 사용자에게 수동 입력 안내                             │
│                                                                 │
│  사용자 알림:                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ⚠️ 자동 데이터 조회에 실패했습니다.                      │   │
│  │                                                         │   │
│  │ 아래 방법으로 데이터를 입력해주세요:                     │   │
│  │ • 네이버 증권에서 복사 후 붙여넣기                       │   │
│  │ • 직접 수동 입력                                        │   │
│  │                                                         │   │
│  │ [네이버 증권 열기] [수동 입력]                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 Cloudflare Worker 한계

| 항목 | 무료 티어 | 대안 |
|------|----------|------|
| 일일 요청 | 100,000회 | 캐싱으로 최소화 |
| 응답 크기 | 10MB | 필요 데이터만 추출 |
| CPU 시간 | 10ms | 간단한 파싱만 수행 |

---

## 결론

### 핵심 요약

1. **자동화 가능 영역 (35점)**
   - PER, PBR, 배당수익률: KRX 정보데이터시스템 활용
   - Cloudflare Worker로 CORS 우회 (무료)

2. **반자동화 영역 (65점)**
   - 클립보드 자동 파싱으로 입력 간소화
   - 드롭다운/라디오버튼으로 정성적 판단 표준화
   - 인기 종목 템플릿으로 반복 입력 제거

3. **사용자 경험**
   - 종목코드 입력 → 자동 데이터 조회 → 나머지 입력 → 즉시 Grade 계산
   - Grade D 종목은 포트폴리오 추가 시 경고
   - 대시보드에서 Grade 기반 필터링

### 예상 결과

```
┌─────────────────────────────────────────────────────────────────┐
│  구현 후 사용자 플로우                                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [기존] 종목 추가 → MCA 파라미터 입력 → 끝                      │
│                                                                 │
│  [개선] 종목 추가                                               │
│         ↓                                                       │
│         종목코드 입력 (005930)                                  │
│         ↓                                                       │
│         🔄 KRX에서 PER/PBR/배당 자동 조회                       │
│         ↓                                                       │
│         나머지 항목 입력 (드롭다운 선택)                        │
│         ↓                                                       │
│         📊 실시간 Grade 계산 표시                               │
│         ↓                                                       │
│         ⚠️ Grade D면 경고: "매수 금지 종목입니다"               │
│         ↓                                                       │
│         저장 → MCA 파라미터 입력 → 끝                          │
│                                                                 │
│  핵심 가치:                                                     │
│  • 감정 배제: 숫자로 검증된 종목만 포트폴리오 편입              │
│  • 자동화: 반복 작업 최소화                                     │
│  • 일관성: 모든 종목에 동일 기준 적용                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

*문서 버전: 1.0*
*작성일: 2026-01-23*
*작성: Claude Opus 4.5*
